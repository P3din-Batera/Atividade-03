let Numero = parseFloat(prompt("Bem vindo!!! Digíte um número e te mostraremos a tabuada dele:"));

Total = 0;

for(let i = 1; i<=10; i++){

    alert(Numero + " x " + i + " = " + (Numero * i));
    
};